[package]
name = "many-literals"
edition = "2024"

[dependencies]
upstream = { path = "upstream", package = "many-literals-upstream", default-features = false }

[features]
vermouth = ["upstream/vermouth"]
dtolnay = ["upstream/dtolnay"]
