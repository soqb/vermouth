[workspace]
members = ["upstream"]

[package]
name = "many-tokens"
edition = "2024"

[dependencies]
upstream = { path = "upstream" }

[features]
vermouth = ["upstream/vermouth"]
dtolnay = ["upstream/dtolnay"]
