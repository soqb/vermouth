[package]
name = "much-nesting"
edition = "2024"

[dependencies]
upstream = { path = "upstream", package = "much-nesting-upstream", default-features = false }

[features]
vermouth = ["upstream/vermouth"]
dtolnay = ["upstream/dtolnay"]
