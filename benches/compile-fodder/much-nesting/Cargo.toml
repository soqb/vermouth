[workspace]
members = ["upstream"]

[package]
name = "much-nesting"
edition = "2024"

[dependencies]
upstream = { path = "upstream" }

[features]
vermouth = ["upstream/vermouth"]
dtolnay = ["upstream/dtolnay"]
