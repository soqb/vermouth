[package]
name = "toy-macros"
description = "a simple test bed & example vermouth macro"
version = "0.0.0"
edition = "2021"
publish = false

[lints]
workspace = true

[lib]
proc-macro = true

[dependencies]
vermouth = { workspace = true, features = ["warnings", "attributes"] }
